<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pts.myapp.dao.CoachDao">
	
	<!-- 코치 신청 -->
	<insert id="createApplication">
		INSERT INTO coaches (uid, profile_photo, career, is_approved)
		VALUES(#{uid}, #{profile_photo}, #{career}, FALSE)
	</insert>
	
	<!-- 코치 신청 리스트 조회 (미승인 상태의 코치 리스트 조회) -->
	<select id="readAllApplication" resultType="com.pts.myapp.dto.CoachDto">
		SELECT id, uid, profile_photo, career, is_approved
		FROM coaches
		WHERE is_approved=FALSE
	</select>
	
	<!-- 코치 승인 -->
	<update id="approve">
		UPDATE coaches
		SET is_approved=TRUE
		WHERE id=#{id}
	</update>
	
	<!-- 코치 수정 -->
	<update id="update">
		UPDATE coaches
		SET profile_photo=#{profile_photo}, career=#{career}
		WHERE id=#{id}
	</update>
	
	<!-- 코치 삭제 -->
	<delete id="delete">
		DELETE
		FROM coaches
		WHERE id=#{id}
	</delete>
	
	<!-- 코치 조회 -->
	<select id="read" resultType="com.pts.myapp.dto.CoachDto">
		SELECT id, uid, profile_photo, career, is_approved
		FROM coaches
		WHERE id=#{id}
	</select>
	
	<!-- 코치 리스트 조회 -->
	<select id="readAll" resultType="com.pts.myapp.dto.CoachDto">
		SELECT id, uid, profile_photo, career, is_approved
		FROM coaches
		WHERE is_approved=TRUE
	</select>

</mapper>